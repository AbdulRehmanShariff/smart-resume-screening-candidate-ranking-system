"""
api/health/__init__.py
----------------------
Health check Blueprint package.

Exports the health_bp Blueprint which is registered by the application
factory under the /api/v1 URL prefix.

Registered route:
  GET /api/v1/health
"""
